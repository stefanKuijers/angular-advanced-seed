angular.module('aas.service.api', [])
    .service('API', [function(Phonestorage) {
        console.log( "API service loaded" );

        // the API object
        return {
        
        };
    }]
);